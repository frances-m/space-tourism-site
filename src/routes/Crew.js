import { Header } from '../common/Header';

import './crew.scss';

export const Crew = () => {
    return (
        <div class="bg bg--crew">
            <Header />
            <h1>Crew</h1>
        </div>
    );
}